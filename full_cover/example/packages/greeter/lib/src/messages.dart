class Messages {
  static String hello(String name) => 'Hello, $name!';

  // Not covered by tests — shows partial coverage within a file
  static String goodbye(String name) => 'Goodbye, $name!';

  static String welcome(String name) => 'Welcome, $name! Glad you are here.';
}
